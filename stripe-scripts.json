{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "stripe:listen": "stripe listen --forward-to localhost:3000/api/payments/webhook",
    "stripe:login": "stripe login",
    "stripe:test": "stripe trigger payment_intent.succeeded"
  }
}
