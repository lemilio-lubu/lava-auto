# railway.toml — Configuracion neutra para monorepo
# Este archivo aplica defaults globales. No fijes dockerfilePath aqui,
# porque puede forzar el Dockerfile incorrecto en servicios distintos.

[deploy]
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3
